defmodule TwitchIrcClient.Models.Privmsg do
    defstruct [:badge_info, :badges, :bits, :color, :display_name, :emotes, :id, :message, :mod, :room_id, :subscriber, :tmi_sent_ts, :turbo, :user_id, :user_type]
end